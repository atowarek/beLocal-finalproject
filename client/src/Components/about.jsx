import React from 'react'
import BottomNavbar from './bottom-navbar'

const About = props => {
  return <div>About the project
    <footer className='page-footer fixed-bottom'>
        <BottomNavbar/>
    </footer>
  </div>
}

export default About
